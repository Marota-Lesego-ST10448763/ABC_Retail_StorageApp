@model ABC_Retail_StorageApp.Models.FileUploadModel

@{
    ViewData["Title"] = "Upload Document";
}

<h2><i class="fas fa-cloud-arrow-up"></i> Upload Document</h2>

<div class="row">
    <div class="col-md-8">
        <div class="card">
            <div class="card-header">
                <h5 class="card-title mb-0">
                    <i class="fas fa-file-upload"></i> Submit Proof of Payment
                </h5>
            </div>
            <div class="card-body">
                <form asp-action="Index" method="post" enctype="multipart/form-data">
                    <div asp-validation-summary="ModelOnly" class="text-danger"></div>

                    <div class="form-group mb-3">
                        <label asp-for="ProofOfPayment" class="form-label">Choose File</label>
                        <input asp-for="ProofOfPayment" type="file" class="form-control" accept=".pdf,.jpg,.jpeg,.png,.doc,.docx" />
                        <span asp-validation-for="ProofOfPayment" class="text-danger"></span>
                        <div class="form-text">Accepted formats: PDF, JPG, PNG, DOC, DOCX (Max size: 10MB)</div>
                    </div>

                    <div class="row">
                        <div class="col-md-6">
                            <div class="form-group mb-3">
                                <label asp-for="OrderID" class="form-label">Order ID (Optional)</label>
                                <input asp-for="OrderID" class="form-control" placeholder="Enter related order ID" />
                            </div>
                        </div>
                        <div class="col-md-6">
                            <div class="form-group mb-3">
                                <label asp-for="CustomerName" class="form-label">Customer Name (Optional)</label>
                                <input asp-for="CustomerName" class="form-control" placeholder="Enter customer name" />
                            </div>
                        </div>
                    </div>

                    <div class="form-group">
                        <button type="submit" class="btn btn-primary btn-lg">
                            <i class="fas fa-upload"></i> Upload File
                        </button>
                        <a asp-controller="Home" asp-action="Index" class="btn btn-secondary">
                            <i class="fas fa-home"></i> Back to Home
                        </a>
                    </div>
                </form>
            </div>
        </div>

        <div class="mt-4">
            <div class="alert alert-info">
                <h6><i class="fas fa-info-circle"></i> How to Upload Your Document:</h6>
                <ul class="mb-0">
                    <li>Click <strong>Choose File</strong> to select your proof of payment or relevant document.</li>
                    <li>Supported formats: <strong>PDF, JPG, PNG, DOC, DOCX</strong> (Maximum size: <strong>10MB</strong>).</li>
                    <li>You may optionally enter an <strong>Order ID</strong> and <strong>Customer Name</strong> to help match the document to the correct transaction.</li>
                    <li>Once ready, click <strong>Upload File</strong> to submit your document securely to Azure Storage.</li>
                    <li>A success message will confirm that your file has been uploaded.</li>
                </ul>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
    <script>
        // File upload preview
        document.getElementById('ProofOfPayment').addEventListener('change', function(e) {
            const file = e.target.files[0];
            if (file) {
                const fileSize = (file.size / 1024 / 1024).toFixed(2);
                console.log(`Selected file: ${file.name} (${fileSize} MB)`);
            }
        });
    </script>
}

